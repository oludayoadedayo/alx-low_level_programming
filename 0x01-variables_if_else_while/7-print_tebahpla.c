#include <stdio.h>

/**
 * main - program that prints the lowercase alphabet in reverse,
 * followed by a new line
 * Return: 0 value
 */
int main(void)
{
	char lowercase alpbt;

	for (lowercase alpbt = 'z'; lowercase alpbt >= 'a'; lowercase alpbt--)
		putchar(lowercase alpbt);

	putchar('\n');
	return (0);
}
